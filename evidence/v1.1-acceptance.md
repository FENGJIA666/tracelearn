# Version 1.1 acceptance - 15 September 2026

Environment: macOS, Apple M4 Pro, 24 GB memory; Codex in-app Chromium browser. All attempts are scripted software checks, not participant data.

## Automated checks

- 22/22 unit tests passed (17 existing, five new); see v1.1-unit-tests.txt.
- All 20 original questions x four choices preserve deterministic grading and source provenance. Malformed/tampered saved history is checked.
- Review-rule tests cover latest-answer replacement, confident errors, ordinary errors and untried transfer questions.
- All 160 display records preserve original answers/errors, citations, timings and input source IDs. Source bodies match the original course. The 80 references and saved reviews remain inspectable.
- Normal TypeScript/Vite production build and portable IIFE build passed. Portable HTML is below 0.5 MB, with inline CSS/JS, no external assets and connect-src 'none'.
- 11 isolated HTTP checks passed: hidden keys, invalid options, wrong/transfer grading, SQLite history, export, prompt bounds, cross-origin and Host rejection, note import and unsupported file rejection.

## Browser observations

- Static preview at 127.0.0.1:4329 served the final self-contained HTML. Intentionally chose the wrong SQL NULL answer with high confidence; exact feedback and null-where source appeared. The transfer answer was graded correctly.
- Notebook prioritized the confident mistake, linked back to its actual question, and retained the original diagnostic issue after a correct transfer answer. A keyboard retry using Space and Enter succeeded.
- Reload restored the stored attempts. Portable learning report was actually downloaded and its contents checked; v1.1-portable-export.md preserves it. The browser tool's download-event wait timed out even though the file arrived.
- Evidence displayed paired outputs and the qualified in-4 typo review; Development / Outside source / out-13 retained the unsupported university-award claim. Filters, source navigation and English/Chinese controls worked.
- Layouts at 320, 768, 1024 and 1440 pixels had matching client/scroll widths and no horizontal page overflow. The narrow layout was visually inspected. New screenshots are actual viewport captures, with no synthetic UI.
- After the final document reload and practice interactions, the browser Resource Timing resource list was empty and console warning/error list was empty. The HTML's connect-src 'none' policy independently disallows API connections. This is a static-preview observation, not a claim that the whole computer was offline.
- Switching from built-in Evidence to an imported course selects Ask the source rather than leaving an empty pane; verified with the original revision-notes.md import.
- Full application at 127.0.0.1:4317 used an isolated acceptance database. New Notebook and Evidence tabs worked. A fresh real qwen3:4b answer to the SQL NULL question returned in 10.1 seconds with matching null-logic and null-where quotations. That single check is not added to the frozen benchmark.

## Explicit boundary

Direct file:// navigation was blocked by the browser tool's safety policy. No bypass was attempted. Direct double-click launch is therefore unverified; the verified interaction path uses localhost static HTTP. Windows/Linux and private-browser storage restrictions were not tested. Portable browser storage may be cleared by the user/browser; keys are embedded for practice, so this is not a secure exam system.

The inference code and all evaluation/* files remain unchanged from v1.0.0. Earlier model/offline measurements are historical evidence and were not rerun or relabeled as new results.
