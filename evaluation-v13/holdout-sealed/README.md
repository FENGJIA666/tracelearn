# Holdout boundary

Do not inspect the questions or references before candidate code, model configuration and scoring rules are frozen. The dataset-author agent holds this split for a later explicitly requested one-shot run. The files are process-sealed on the shared filesystem, not encrypted.

Only counts, category/language distributions and SHA-256 hashes may be reported to the candidate implementer before freezing. No model has been run on this split at dataset lock time.
